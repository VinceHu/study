# 发布指南

## ✅ Git 仓库已初始化

本地 git 仓库已经创建并完成首次提交！

### 当前状态

- ✅ Git 仓库已初始化
- ✅ 所有文件已提交
- ✅ 远程仓库已配置：`git@github.com:VinceHu/study.git`
- ✅ 分支已设置为 `main`

### 推送到 GitHub

由于 SSH 连接问题，请手动执行推送：

```bash
cd interview-guide
git push -u origin main
```

如果 SSH 连接失败，可以使用 HTTPS：

```bash
# 切换到 HTTPS
git remote set-url origin https://github.com/VinceHu/study.git

# 推送
git push -u origin main
```

### 首次推送后

推送成功后，访问：
- 仓库地址：https://github.com/VinceHu/study
- 在线文档：https://vincehu.github.io/study （GitHub Pages 自动部署）

### 启用 GitHub Pages

1. 进入仓库设置：https://github.com/VinceHu/study/settings/pages
2. 在 **Source** 下选择 **GitHub Actions**
3. 保存设置

等待几分钟，GitHub Actions 会自动构建并部署网站。

### 后续更新

每次添加新题目或修改内容后：

```bash
# 1. 添加更改
git add .

# 2. 提交
git commit -m "feat: 添加新题目 - XXX"

# 3. 推送
git push
```

推送后会自动触发部署，几分钟后在线文档就会更新。

## 📦 版本管理

### 创建版本标签

```bash
# 创建版本标签
git tag -a v1.0.0 -m "Release v1.0.0 - 初始版本

- 14道面试题
- VitePress 静态网站
- 自动统计功能
- GitHub Actions 自动部署"

# 推送标签
git push origin v1.0.0
```

### 版本号规范

使用语义化版本号（Semantic Versioning）：

- **主版本号（Major）**：不兼容的 API 修改
- **次版本号（Minor）**：向下兼容的功能性新增
- **修订号（Patch）**：向下兼容的问题修正

示例：
- `v1.0.0` - 初始版本
- `v1.1.0` - 添加新题目（功能新增）
- `v1.1.1` - 修复错误（问题修正）
- `v2.0.0` - 重大重构（不兼容修改）

### 提交信息规范

使用约定式提交（Conventional Commits）：

```bash
# 新功能
git commit -m "feat: 添加Promise详解题目"

# 修复问题
git commit -m "fix: 修复代码示例错误"

# 文档更新
git commit -m "docs: 更新README"

# 样式调整
git commit -m "style: 优化首页样式"

# 重构
git commit -m "refactor: 重构统计组件"

# 性能优化
git commit -m "perf: 优化图片加载"

# 测试
git commit -m "test: 添加单元测试"

# 构建
git commit -m "build: 更新依赖"

# CI/CD
git commit -m "ci: 更新部署配置"
```

## 🚀 部署状态检查

### 查看部署状态

1. 访问 Actions 页面：https://github.com/VinceHu/study/actions
2. 查看最新的工作流运行状态
3. 如果失败，点击查看详细日志

### 常见问题

#### 1. 推送失败

**问题：** `Permission denied (publickey)`

**解决：**
```bash
# 检查 SSH 密钥
ssh -T git@github.com

# 如果失败，使用 HTTPS
git remote set-url origin https://github.com/VinceHu/study.git
```

#### 2. GitHub Pages 404

**问题：** 访问网站显示 404

**解决：**
1. 检查 GitHub Pages 是否启用
2. 检查 `.vitepress/config.mts` 中的 `base` 配置
3. 等待几分钟让部署完成

#### 3. 构建失败

**问题：** GitHub Actions 构建失败

**解决：**
1. 查看 Actions 日志
2. 检查 `package.json` 依赖
3. 本地运行 `npm run docs:build` 测试

## 📊 项目信息

### 当前版本

- **版本号：** v1.0.0
- **提交数：** 1
- **文件数：** 41
- **题目数：** 14

### 项目统计

```bash
# 查看提交历史
git log --oneline

# 查看文件统计
git ls-files | wc -l

# 查看代码行数
git ls-files | xargs wc -l
```

## 🎯 下一步

1. **推送代码**
   ```bash
   git push -u origin main
   ```

2. **启用 GitHub Pages**
   - 进入仓库设置
   - 选择 GitHub Actions 作为部署源

3. **等待部署**
   - 查看 Actions 页面
   - 等待构建完成

4. **访问网站**
   - https://vincehu.github.io/study

5. **分享链接**
   - 将网站分享给其他人

## 📝 更新日志

### v1.0.0 (2025-11-20)

**新增功能：**
- ✨ 完成14道面试题（CSS 5题、JavaScript 7题、Vue 2题）
- ✨ 集成 VitePress 构建静态网站
- ✨ 实现自动统计题目数量
- ✨ 配置 GitHub Actions 自动部署
- ✨ 添加 Promise 详解题目
- ✨ 优化首页和导航体验

**技术栈：**
- VitePress 1.6.4
- Vue 3
- GitHub Actions
- GitHub Pages

**文档：**
- README.md - 项目说明
- VITEPRESS_GUIDE.md - VitePress 使用指南
- DEPLOYMENT.md - 部署指南
- HOW_TO_ADD_QUESTION.md - 添加题目指南
- AUTO_COUNT_GUIDE.md - 自动统计说明

---

**提示：** 推送代码后，记得在 GitHub 仓库设置中启用 GitHub Pages！
